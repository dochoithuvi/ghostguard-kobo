# Controller presets

Reserved for controller-specific defaults derived from validated Kobo datasets.
A preset must never override the fail-open policy and must record the controller
fingerprint/evidence used to justify the tuning.
